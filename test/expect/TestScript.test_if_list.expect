graph(%x : Double(*, *)) {
  %2 : int = prim::Constant[value=1]()
  %c : Dynamic[] = prim::If(%2)
    block0() {
      %c.1 : Dynamic[] = prim::ListConstruct(%x, %x)
      -> (%c.1)
    }
    block1() {
      %c.2 : Dynamic[] = prim::ListConstruct(%x, %x, %x)
      -> (%c.2)
    }
  %8 : int = prim::Constant[value=0]()
  %9 : Dynamic = aten::cat(%c, %8)
  return (%9);
}
