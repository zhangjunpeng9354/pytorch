graph(%x : Float()
      %y : Float()) {
  %3 : Float() = prim::FusionGroup_0[device=-1](%y, %x)
  return (%3);
}
with prim::FusionGroup_0 = graph(%1 : Float()
      %2 : Float()) {
  %3 : int = prim::Constant[value=2]()
  %4 : Float() = aten::mul(%3, %2)
  %5 : int = prim::Constant[value=1]()
  %6 : Float() = aten::add(%4, %1, %5)
  return (%6);
}
