graph(%x.1_data : Dynamic
      %x.1_mask : Dynamic
      %x.1_dims : Dynamic
      %y_data : Dynamic
      %y_mask : Dynamic
      %y_dims : Dynamic) {
  %7 : int = prim::Constant[value=10]()
  %8 : int = prim::Constant[value=1]()
  %x : Dynamic, %10 : Dynamic, %11 : Dynamic = prim::Loop(%7, %8, %x.1_data, %x.1_mask, %x.1_dims)
    block0(%loop_num : int, %7_data : Dynamic, %7_mask : Dynamic, %7_dims : Dynamic) {
      %17 : int = prim::Constant[value=1]()
      %18 : Long() = prim::NumToTensor(%17)
      %alpha : float = prim::TensorToNum(%18)
      %data.1 : Dynamic = aten::add(%7_data, %y_data, %alpha)
      %mask : Dynamic = aten::mul(%7_mask, %y_mask)
      %dims : Dynamic = aten::__or__(%7_dims, %y_dims)
      %23 : int = prim::Constant[value=1]()
      %data : Dynamic = aten::where(%mask, %data.1, %7_data)
      -> (%23, %data, %mask, %dims)
    }
  return (%x, %10, %11);
}
